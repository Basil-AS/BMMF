# 🧮 Математический алгоритм FMPF

## 1. Теоретические основы

### 1.1 Постановка задачи

Задача **Optimal Meeting Point (OMP)** — найти точку $p^*$ такую, что времена в пути от N участников максимально близки друг к другу.

**Дано:**
- $U = \{u_1, u_2, ..., u_n\}$ — координаты участников
- $T(u_i, p)$ — функция времени в пути от $u_i$ до точки $p$
- $M_i \in \{\text{transit}, \text{auto}\}$ — режим транспорта участника $i$

**Найти:**
$$p^* = \arg\min_p F(p)$$

где $F(p)$ — целевая функция (метрика справедливости).

### 1.2 Метрики справедливости

#### Range (Размах)
$$F_{range}(p) = \max_i T_i - \min_i T_i$$

Минимизирует разницу между самым долгим и самым коротким путём.

**Свойства:**
- Простая интерпретация
- Чувствительна к выбросам
- Не учитывает распределение средних значений

#### MaxDev (Максимальное отклонение)
$$F_{maxdev}(p) = \max_i |T_i - \bar{T}|$$

где $\bar{T} = \frac{1}{n}\sum_i T_i$ — среднее время.

**Свойства:**
- Учитывает отклонение каждого участника
- Более устойчива к выбросам

#### Variance (Дисперсия)
$$F_{var}(p) = \frac{1}{n}\sum_i (T_i - \bar{T})^2$$

**Свойства:**
- Статистически оптимальна
- Штрафует большие отклонения квадратично
- Хороша для математического анализа

#### MiniMax
$$F_{minimax}(p) = \max_i T_i$$

Минимизирует максимальное время в пути.

**Свойства:**
- Гарантирует, что никто не едет слишком долго
- Не учитывает справедливость напрямую

#### Composite (Композитная)
$$F_{comp}(p) = \alpha \cdot \max_i T_i + \beta \cdot \sigma(T) + \gamma \cdot R(T)$$

где:
- $\sigma(T)$ — стандартное отклонение
- $R(T) = \max T - \min T$ — размах
- $\alpha, \beta, \gamma$ — весовые коэффициенты (по умолчанию 1, 2, 1)

**Свойства:**
- Учитывает несколько факторов
- Настраиваемая
- Рекомендуется для большинства случаев

## 2. Алгоритм оптимизации

### 2.1 Проблема вычислительной сложности

Наивный подход (Brute Force):
- Сетка 100×100 = 10,000 точек
- N участников × 10,000 точек = 10,000N запросов к API
- При N=3: **30,000 запросов** → $15-150 в зависимости от тарифа

**Решение:** Многофазная оптимизация с математическими предрасчётами.

### 2.2 Трёхфазный алгоритм

```
┌─────────────────────────────────────────────────────────────┐
│                    ФАЗА 1: Грубый поиск                     │
├─────────────────────────────────────────────────────────────┤
│ 1. Вычисляем центроид C = (Σlat/n, Σlon/n)                  │
│ 2. Строим Bounding Box с расширением                        │
│ 3. Генерируем сетку NxN кандидатов                          │
│ 4. Предфильтруем геометрически (убираем слишком далёкие)    │
│ 5. Запрашиваем маршруты для всех кандидатов                 │
│ 6. Выбираем топ-3 по метрике                                │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                 ФАЗА 2: Адаптивное уточнение                │
├─────────────────────────────────────────────────────────────┤
│ 1. Для каждого из топ-3 генерируем микросетку 3×3           │
│    в радиусе ~500м                                          │
│ 2. Запрашиваем маршруты                                     │
│ 3. Обновляем лучший результат                               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                 ФАЗА 3: Локальная оптимизация               │
├─────────────────────────────────────────────────────────────┤
│ 1. Исследуем 8 направлений от лучшего кандидата             │
│    (N, S, E, W, NE, NW, SE, SW) на ~200м                    │
│ 2. Применяем принцип Golden Section Search                  │
│ 3. Финализируем результат                                   │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 Геометрическая предфильтрация

Убираем кандидатов, которые заведомо плохи:

```javascript
prefilterCandidates(candidates, points, maxDistKm = 30) {
  const centroid = Math2.centroid(points);
  
  return candidates.filter(c => {
    // 1. Слишком далеко от центроида
    if (haversine(c, centroid) > maxDistKm) return false;
    
    // 2. Дальше от любой точки, чем самая далёкая точка от центроида × 2.5
    const maxUserDist = max(points.map(p => haversine(p, centroid)));
    for (const p of points) {
      if (haversine(c, p) > maxUserDist * 2.5) return false;
    }
    
    return true;
  });
}
```

**Экономия:** 30-50% запросов на первой фазе.

### 2.4 Формула Гаверсинуса

Для расчёта расстояний на сфере:

$$d = 2R \cdot \arcsin\left(\sqrt{\sin^2\left(\frac{\Delta\phi}{2}\right) + \cos(\phi_1)\cos(\phi_2)\sin^2\left(\frac{\Delta\lambda}{2}\right)}\right)$$

где:
- $R = 6371$ км (радиус Земли)
- $\phi_1, \phi_2$ — широты в радианах
- $\Delta\phi = \phi_2 - \phi_1$
- $\Delta\lambda = \lambda_2 - \lambda_1$

## 3. Оценка количества запросов

### 3.1 Формула

$$Q_{total} = Q_{phase1} + Q_{phase2} + Q_{phase3}$$

где:
- $Q_{phase1} = (G^2 + 1) \cdot N \cdot f_{prefilter}$
- $Q_{phase2} \approx 3 \cdot 9 \cdot N = 27N$
- $Q_{phase3} \approx 8N$

$G$ — размер сетки, $N$ — количество участников, $f_{prefilter} \approx 0.7$.

### 3.2 Примеры

| Участники | Сетка | Phase 1 | Phase 2 | Phase 3 | **Всего** | Brute Force | Экономия |
|-----------|-------|---------|---------|---------|-----------|-------------|----------|
| 3 | 6×6 | 78 | 81 | 24 | **183** | 900 | 80% |
| 4 | 6×6 | 104 | 108 | 32 | **244** | 1200 | 80% |
| 5 | 6×6 | 130 | 135 | 40 | **305** | 1500 | 80% |
| 3 | 8×8 | 137 | 81 | 24 | **242** | 1500 | 84% |

## 4. Особенности Yandex Maps API

### 4.1 Режим masstransit

Yandex Maps API в режиме `masstransit` учитывает:

| Тип | API Name | Расписание |
|-----|----------|------------|
| Метро | `underground` | ✅ Да |
| МЦД | `suburban` | ✅ Да |
| Электрички | `suburban` | ✅ Да |
| Автобусы | `bus` | ✅ Да |
| Троллейбусы | `trolleybus` | ✅ Да |
| Трамваи | `tram` | ✅ Да |
| Маршрутки | `minibus` | ⚠️ Частично |

### 4.2 Структура ответа

```javascript
route.getPaths().each(path => {
  path.getSegments().each(segment => {
    const props = segment.properties.getAll();
    // props.type: 'pedestrian', 'masstransit', 'transfer'
    // props.duration: { value: 120, text: '2 мин' }
    // props.transports: [{ type: 'bus', name: 'А11' }]
  });
});
```

### 4.3 Ограничения

- **Время ожидания:** Запрос маршрута может занять 2-10 секунд
- **Ночные маршруты:** Без времени отправления ОТ может быть недоступен
- **Лимиты:** Зависят от тарифа (1000-100000+ запросов/сутки)

## 5. Рекомендации по параметрам

### 5.1 Размер сетки

| Ситуация | Сетка | Обоснование |
|----------|-------|-------------|
| Близкие точки (<5 км) | 4×4 | Высокая плотность |
| Средние (~10 км) | 6×6 | Баланс |
| Далёкие (>15 км) | 8×8 | Широкий охват |

### 5.2 Расширение сетки

| % | Когда использовать |
|---|-------------------|
| 10-15% | Точки равномерно распределены |
| 20-25% | Стандартная ситуация |
| 30-40% | Точки сильно разбросаны |

### 5.3 Метрика

| Метрика | Когда использовать |
|---------|-------------------|
| `composite` | Почти всегда |
| `range` | Критична минимальная разница |
| `minimax` | Важнее скорость, чем справедливость |
| `variance` | Научные/статистические задачи |

## 6. Планы развития

### 6.1 Интеграция изохрон

```
1. Построить изохроны I(u_i, τ) через Valhalla/TravelTime
2. Найти пересечение S = ∩ I(u_i, τ)
3. Искать кандидатов только внутри S
4. Валидировать через Yandex API
```

**Потенциальная экономия:** 90%+ запросов к Yandex.

### 6.2 Машинное обучение

- Обучить модель предсказания времени в пути по геометрии
- Использовать для предфильтрации
- Запрашивать API только для финальных кандидатов

## 7. Ссылки

1. Weber Problem (1909) — основа задачи OMP
2. Haversine Formula — расчёт расстояний на сфере
3. Golden Section Search — оптимизация без производных
4. [Yandex Maps API Documentation](https://yandex.ru/dev/maps/)
